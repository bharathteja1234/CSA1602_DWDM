input <- readline(prompt = "Enter comma-separated values: ")
data <- as.numeric(unlist(strsplit(input, ",")))
if (all(!is.na(data))) {
  median_value <- median(data)
    cat("Median:", median_value)
} else {
  cat("Invalid input. Please enter numeric values separated by commas.")
}
